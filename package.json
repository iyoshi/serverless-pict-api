{
  "name": "serverless-pict-api",
  "version": "0.0.1",
  "dependencies": {},
  "devDependencies": {
    "serverless": "^1.30.3",
    "serverless-python-requirements": "^4.1.1",
    "serverless-wsgi": "^1.5.1"
  },
  "scripts": {
    "setup": "npm install && pipenv install",
    "lint": "pipenv run pylint ./src/",
    "test": "pipenv run pytest",
    "deploy": "serverless deploy"
  }
}
